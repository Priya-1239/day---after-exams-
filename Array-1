#include <stdio.h>
int main() {
   int n,i,a[10],sum=0 ;
   scanf("%d",&n);
   for(i=0;i<n;i++){
       scanf("%d",&a[i]);
   }
   printf("Elements of array:");
   for(i=0;i<n;i++){
       printf("%d\n",a[i]);
       sum=sum+a[i];
   }
    printf("\n%d",sum);
    return 0;
}
